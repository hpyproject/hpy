# Template to install latest Python 3
# -----------------------------------
#
# Also:
# - updates pip and wheel

steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.8'
    displayName: 'Use Python 3.8'
  - script: python -m pip install --upgrade pip wheel
    displayName: 'Update pip and wheel'
